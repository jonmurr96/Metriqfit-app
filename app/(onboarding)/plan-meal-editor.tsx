/**
 * Onboarding Plan Meal Editor
 * 
 * This is a wrapper around the tabs nutrition meal editor
 * that keeps the navigation within the onboarding group.
 */

import PlanMealEditor from '../(tabs)/nutrition/plan-meal-editor';

export default PlanMealEditor;
