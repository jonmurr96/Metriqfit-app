/**
 * program-browser.tsx
 * 
 * Program browser screen - now uses the new V2 component with scientific exercise selection.
 */

import ProgramBrowserV2 from '../../../components/programs/ProgramBrowserV2';

export default function ProgramBrowserScreen() {
  return <ProgramBrowserV2 />;
}
